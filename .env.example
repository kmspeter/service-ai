# Phase 01 application settings
APP_NAME=service-ai
ENVIRONMENT=development
LOG_LEVEL=INFO
HOST=127.0.0.1
PORT=8000

# Future phase settings. Uncomment only when the related phase is implemented.
# LLM_PROVIDER=openai
# LLM_API_KEY=replace-with-a-real-secret
# LLM_MODEL=replace-with-a-model-name
# EMBEDDING_MODEL=replace-with-an-embedding-model
# QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=replace-with-a-real-secret
# QDRANT_COLLECTION=documents
# MINIO_URL=http://localhost:9000
# MINIO_ACCESS_KEY=replace-with-a-real-secret
# MINIO_SECRET_KEY=replace-with-a-real-secret
# MINIO_BUCKET=documents

